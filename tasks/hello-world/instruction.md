# Task: Hello World

Create a Python script at `/workspace/hello.py` that prints exactly:

```
Hello, World!
```

## Requirements

- The script must be located at `/workspace/hello.py`
- When executed with `python hello.py`, it must output exactly `Hello, World!` (with the comma and exclamation mark)
- No additional output or whitespace
